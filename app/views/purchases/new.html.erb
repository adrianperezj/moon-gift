<h1>New Purchase</h1>


<%= simple_form_for [@event, @gift, @purchase] do |f| %>
  <%= f.input :payment_method, collection: ["Credit Card", "Debit Card"], label: "Payment Method:" %>
  <%= f.submit "Submit purchase", class: "btn btn-primary" %>
<% end %>
